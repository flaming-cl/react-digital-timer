import { FC } from 'react';
interface iconProps {
    alt: string;
    className: string;
    src: string;
    onClick: () => void;
}
declare const Button: FC<iconProps>;
export default Button;
